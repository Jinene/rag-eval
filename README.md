# RAG Evaluation Demo

This project demonstrates a Retrieval Augmented Generation (RAG) pipeline and evaluates answer quality using RAGAS metrics.

Metrics measured:
- answer relevancy
- faithfulness
- context precision

## Install

pip install -r requirements.txt

## Run

python main.py

Results will be stored in results.csv
